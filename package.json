{
  "name": "@browserql/merge-schemas",
  "version": "1.0.18",
  "license": "MIT",
  "main": "index.js",
  "scripts": {
    "test": "yarn build && npx ts-node ./test.ts",
    "deploy": "yarn test && node deploy",
    "build": "tsc"
  },
  "peerDependencies": {
    "graphql": "^16.0.1"
  },
  "devDependencies": {
    "@types/node": "^16.11.7",
    "typescript": "^4.4.4"
  },
  "dependencies": {
    "graphql": "^16.0.1",
    "semver": "^7.3.5"
  }
}